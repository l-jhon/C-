// Jhon Lucas Santos Silva 
// Programa que, a partir da leitura da placa do carro, informe o m�s em que o emplacamento deve ser renovado
#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	int aux, np, /* numero da placa*/ 
	un, /*ultimo numrero da placa*/
	un2,/*dois ultimos numeros*/
	mr; /*mes em que o emplacamento dever ser renovado*/
	cout<<" Digite o numero da placa:";
	cint>>np;
	aux= np/10;
	un=aux%10;
	un2= 
	if(un==1||un==2||un==3||un==4||un==5||un==6||un==7||un==8||un==9||un==10||un==11||un==12)
		cout<<"Mês de renovaçao do emplacamento";<<un<<endl;
	if(|un> 9) 
	
	
	
	
